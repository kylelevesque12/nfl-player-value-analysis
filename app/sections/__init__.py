"""One module per navigable section of the app.

Deliberately NOT named ``pages``: Streamlit treats a ``pages/`` directory
sitting beside the entry script as a multipage app and builds its own
navigation from it, which would collide with this app's single sidebar radio.
"""
