# NFL Player Value Analysis - dependencies
#
# Python 3.10+ is required (3.11+ recommended). The data-collection package
# nflreadpy requires 3.10+, and the pinned scientific stack below targets
# modern interpreters. The legacy .venv in this repo is Python 3.9 and should
# be recreated:  python3.11 -m venv .venv && pip install -r requirements.txt
#
# Versions use compatible-release pins (~=) so patch updates are allowed but
# breaking minor/major bumps are not, which keeps results reproducible without
# freezing the project to a single point release.

# Core data + numerics
pandas~=2.3
numpy~=2.3
scikit-learn~=1.6

# Visualization
matplotlib~=3.10
seaborn~=0.13
plotly~=6.5

# Notebooks
jupyter~=1.1
ipykernel~=7.1

# Excel output
openpyxl~=3.1

# App layer
streamlit~=1.58

# Advanced modeling track (Optuna search, SHAP, MLflow tracking, Polars profile)
optuna~=4.5
shap~=0.48
mlflow~=3.6
polars~=1.35

# Raw data collection (requires Python 3.10+)
nflreadpy; python_version >= "3.10"

# Testing
pytest~=8.4
