# External Benchmark

No external projections were available at the expected path (`data/raw/external_projections.csv`). See `PORTFOLIO_ROADMAP.md` Tier 1 item #1 for acquisition options. Drop a CSV with columns `season,week,player_id,external_projection_ppr` and re-run `python scripts/run_pipeline.py --steps external_benchmark`.
